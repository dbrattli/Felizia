// <auto-generated />

/// Theme handling is a bit tricky client side as we cannot use reflection to map a string to a type as we do server
/// side. This file will be get theme information added and written as `Theme.fs`. If you know a better way to do this
/// then please open an issue.
module Felizia.Theme

open Felizia
open Felizia.Arctic

let theme = {
    Name = "Felizia.Arctic"
    Index = Partials.Index.index
    Single = Partials.SingleView.singleView
    List = Partials.ListView.listView
}
